# E079: KC 신용할당 — 시냅스별 R-STDP로 교체

- **등록일**: 2026-09-16
- **상태**: 등록됨
- **검증할 가설**: H013 (KC 학습에 신용 할당이 없다)
- **경쟁 가설**: H013-null (시냅스별로 바꿔도 std가 안 오르거나 행동 효과 없음)

## 배경
`kc_learning_probe`(2026-09-16): KC는 학습하지만 **std(후)=0.0000** — 모든 시냅스가 똑같이 변한다.
갱신식(forager_brain.py 5509): `w_d1 += eta_d1 * d1_trace * dopamine_level`, `d1_trace`는 **집단 스칼라**.
C35의 피질 학습과 동일 구조. 초파리 버섯체를 **스파스 코딩** 때문에 차용했는데
학습이 그 분리를 구분하지 못하면 차용 목적이 무산된다.

**E077은 중단했다** — 배선(K16)과 학습기구(K17)가 고장난 상태에서 환경 효과를 재는 것은 순서가 뒤바뀜.

## 1. 조작
`--kc-rstdp`: `kc_to_d1_l/r`을 **시냅스별 자격흔적 R-STDP**로 교체(배선 완료, `rstdp_model.py` 재사용).
- 기본(정적 스칼라) vs `--kc-rstdp`(시냅스별), 뇌 5반복 × (수리/정적) = **20런**
- 공통: `--real-rstdp --crossed --epsilon 0.6 --bias 25 --d1-inhib -400 --direct-inhib -100 --reflex-w 3 --episodes 60`

## 2. 조작 검증  ★필수 — C36과 동일 방식
1. **도파민 OFF에서 |Δ|=0** (게이팅 작동)
2. **도파민 ON에서 std 0→증가**, 변화 시냅스가 **100% 미만** (일부만 변함 = 신용 할당)
   — 현재는 변화율 100% / std 0.0000이므로, 이것이 바뀌지 않으면 조작 무효
3. 두 조건 결과가 소수점까지 같으면 조작 무효 의심(전례 5회)

## 3. 불변식 확인
> `[x]` 준수 / `[~]` 선언된 이탈(사유 필수) / `[ ]` 미확인 → 게이트 거부
- [x] INV-A1 genn_seed 고정
- [x] INV-A2 random/np.random 시드 고정
- [x] INV-A3 d1_to_direct_weight = 20
- [x] INV-A4 d1_inhibition = -400
- [x] INV-A5 direct_inhibition = -100
- [x] INV-A6 evaluate 상태 정규화
- [x] INV-B1 변조폭 지표
- [x] INV-B3 같은 시드 짝짓기

## 4. 사전 판정 기준
**(a) 신용 할당 성립** — 조작 검증 단계:
- **지지**: std(후) > 0.1 **그리고** 변화 시냅스 비율 < 95%
- **기각**: std(후) < 0.01 또는 변화율 ≥ 99% → 시냅스별 전환이 안 됨

**(b) 행동 효과** — (a) 통과 시:
- **H013 지지**: `--kc-rstdp`의 (수리−정적)이 기본보다 **0.008 이상 더 음수**, 5뇌 중 3 이상 방향 일치
- **H013 기각**: 차이 **0.003 이내** → 신용 할당이 생겨도 행동은 안 변함
- 방향이 갈리면 8뇌로 확장

## 5. 이 결과로 새로 알게 되는 것
- **(a)(b) 모두 지지** → 스파스 코딩이 비로소 학습에 쓰인다. 버섯체 차용이 처음으로 기능하며,
  CHARTER의 **일반화** 요건을 시험할 기반이 생긴다.
- **(a) 지지, (b) 기각** → 신용 할당은 됐으나 행동에 안 닿는다. 병목이 KC 하류(D1 2개 수렴, K16)에 있다는
  뜻이며, FlyWire 실측(MBON 96개, 상호억제 50%)을 따라 **출력단을 늘리는** 설계로 간다.
- **(a) 기각** → 배선이 실제로 안 바뀐 것. 조작을 고친다.

---
## 실행 결과
- **실행 커맨드**: 조작검증 `scripts/e079_check.sh` / 본실험 `scripts/e079_main.sh`
- **원본 로그**: 아래 「조작 검증 결과」·「본 실험 결과」 절
- **결과**: (a) 신용할당 std 0→4.33, 변화율 100%→29.8% / (b) 행동 (수리−정적) 차이 **+0.0005**
- **판정**: **(a) 지지 / (b) 기각**
  - ⚠ **2026-09-16 단서**: (a)의 조작검증은 `kc_learning_probe.py`가 `d1_inhibition`을
    설정하지 않은 채 돌았다(기본값 0.0 = D1 포화). **INV-A4 위반** — 규약 P15 참조.
    R-STDP 자격흔적은 발화에 의존하므로 (a)의 수치는 **재측정 필요**.
    (b)는 `e079_main.sh`가 `--d1-inhib -400 --direct-inhib -100`을 썼으므로 **유효**.
- **갱신한 곳**: K17·K18(current-state), H013, H014 신설

---
## 조작 검증 결과 (2026-09-16) — **통과**
| 조건 | 시냅스 | \|Δ\|평균 | 변화율 | **std(후)** |
|---|---|---|---|---|
| 기본(집단 스칼라) | kc_to_d1_l | 0.0020 | **100.0%** | **0.0000** |
| 기본 | kc_to_d1_r | 0.0020 | 100.0% | 0.0000 |
| **--kc-rstdp** | **kc_to_d1_l** | 1.911 | **29.8%** | **4.3261** |
| --kc-rstdp | kc_to_d1_r | 5.351 | 100.0% | **3.8779** |

**사전 기준(std > 0.1 AND 변화율 < 95%)** → `kc_to_d1_l`이 충족(29.8% / std 4.33).
우측은 변화율 100%지만 std 3.88로 **시냅스별 분화는 발생**(활동량 차이로 추정).

→ **신용 할당이 처음으로 생겼다.** 스파스 코딩으로 분리한 패턴을 학습이 구분한다.

### ⚠ 부작용 관찰
`food_to_d1_l`이 기본 0.0560 → kc-rstdp **0.0000**으로 떨어졌다.
KC 경로가 강해지면서 D1 활동이 바뀌어 `food_to_d1` 학습 조건이 미충족됐을 가능성.
**본 실험에서 함께 추적한다** — 한 경로를 살리고 다른 경로를 죽이면 순이득이 없다.

---
## 본 실험 결과 (20런, 2026-09-16)

### (수리 − 정적) 효과
| 뇌 | 기본 | KC학습 | 차이 |
|---|---|---|---|
| b0 | −0.0113 | −0.0109 | +0.0004 |
| b1 | −0.0005 | −0.0001 | +0.0004 |
| b2 | +0.0030 | +0.0028 | −0.0002 |
| b3 | −0.0184 | −0.0162 | +0.0022 |
| b4 | −0.0054 | −0.0056 | −0.0002 |
| **평균** | **−0.0065** | **−0.0060** | **+0.0005** |

### 판정: **(a) 지지 / (b) 기각**
| 사전 기준 | 실측 | 결과 |
|---|---|---|
| (a) 신용할당: std>0.1 & 변화율<95% | std **4.33**, 변화율 **29.8%** | **지지** |
| (b) 행동: 0.008 이상 더 음수 | **+0.0005** (반대 방향) | 미달 |
| (b) 기각: 0.003 이내 | 0.0005 | **해당** |

**신용 할당은 생겼으나 행동은 변하지 않는다.** 5뇌 중 3뇌가 오히려 양수 방향.

### 사전등록이 예고한 해석
> "(a) 지지, (b) 기각 → 신용 할당은 됐으나 행동에 안 닿는다. 병목이 **KC 하류**(D1 2개 수렴, K16)에
> 있다는 뜻이며, FlyWire 실측(MBON 96개, 상호억제 50%)을 따라 **출력단을 늘리는** 설계로 간다."

이 분기가 발동했다. KC가 아무리 잘 구분해도 **D1 좌/우 2개로 수렴하면 평균화**된다.
FlyWire 실측: MBON **96개**가 KC 5,177개를 받고(MBON당 441개), MBON끼리 **50% 억제**로 경쟁한다.
우리는 출력이 2개뿐이고 경쟁 구조도 없다.

### 갱신한 곳
`hypotheses/H013.md`(부분지지/행동기각), `current-state.md`, H014 신설

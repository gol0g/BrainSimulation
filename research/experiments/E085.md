# E085: 이득 효과의 정체 — 다른 경로 학습의 증폭인가

- **등록일**: 2026-09-19
- **상태**: **재설계 (2026-09-19 16:49)** — 1차 설계는 배선이 칸마다 달라 중단(0/20런)
- **검증할 가설**: H016 (D1 하류 전달 이득이 병목 — 이득은 학습에 비특이적으로 작용한다)
- **경쟁 가설**: H016-null(이득은 학습과 무관하게 행동을 바꾼다), H004(KC 경로 특이적)

## 배경 — E084가 남긴 미해결
E084에서 KC 학습을 **하나도 켜지 않고** `kc_d1_w`만 0.5→150으로 올린 조건(C−A)이
평균 **−0.0272**로, 고이득에서 KC 학습을 켠 효과(−0.0148)보다 **컸다**.

네 칸 모두 `--real-rstdp --crossed`(다른 경로 학습)가 켜져 있었으므로 두 설명이 남는다.
1. **증폭 설명**: 이득이 *다른 경로의 학습*을 증폭했다 → 학습을 다 끄면 (c)가 사라져야 한다.
2. **비학습 설명**: 이득 자체가 학습과 무관하게 행동을 바꾼다 → 학습을 다 꺼도 (c)가 남는다.

이 둘은 다음 수를 완전히 갈라놓는다. 1이면 "하류 증폭률"이 병목으로 특정되고,
2라면 우리가 재는 "변조폭 변화"가 학습 지표가 아니라는 뜻이다.

## 1. 조작
`kc_d1_w` 0.5 / 150 × **모든 가소성 끔 / 다른경로만 켬** 2×2. KC 학습은 네 칸 모두 **끈다**.

> **1차 설계 철회**: `off = --no-reward`(교차 시냅스 **없음**) vs `other = --real-rstdp --crossed`
> (교차 시냅스 **있음**)로 잡아, 학습 유무와 **배선 구조가 함께** 바뀌었다.
> `--crossed`는 `food_eye_L→D1_R` 시냅스 집단을 **생성**한다(`forager_brain.py:2952`).
> 외부 검토 지적 #3. 0/20런에서 중단.

**재설계**: 네 칸 모두 `--real-rstdp --crossed`를 켜서 **배선을 동일하게** 두고,
보상만 끊어(`--no-reward`) 가소성을 멈춘다. R-STDP는 도파민으로 게이팅되므로 보상이 없으면 갱신이 없다.

| | 학습 끔 (`--no-reward`) | 학습 켬 |
|---|---|---|
| **kc_d1_w = 0.5** | P | Q |
| **kc_d1_w = 150** | R | S |

네 칸 공통: `--real-rstdp --crossed --kc-w-max 750 --bias-at-d1 --transplant-eval`,
KC 학습(`--kc-rstdp`)은 네 칸 모두 **끔**.

- (R−P) = **학습이 전혀 없을 때의 이득 효과**
- (S−Q) = E084의 (C−A) 재현
- 뇌 5반복, `--env-seed 0`, `--transplant-eval`, `--bias-at-d1`.  = **20런**

## 2. 조작 검증  ★필수
1. **가소성 정지 확인**: P·R 칸에서 **이식 목록 전체**(뇌에서 유도한 `_rstdp_synapses`)의
   학습 전후 |Δ| = 0. **0이 아니면 조작 무효.** 목록을 손으로 적지 않는다(2026-09-19 사고).
1b. **이식 완전성**: 매 런 로그의 `[이식] N개 경로: ...` 줄에 교차 경로가 포함되는지 확인한다.
2. **이득 조작**: `kc_to_d1` 평균이 0.5 / 150으로 유지되는가(상한 눌림 없음).
3. **연결 동일성**: 네 칸 지문 일치를 **본실험과 같은 설정**(`--real-rstdp --crossed` 포함)으로
   확인한다. E084의 확인은 real/crossed를 끈 설정이었으므로 **증거로 쓸 수 없다**(검토 지적 #3).
3b. **과제 경로 이식 항등성**: `--episodes 0`에서 사전 = 사후(이식), 5개 시드 전부.
   **완료 (2026-09-19 16:49)**: +0.0973 / +0.1045 / +0.0921 / +0.1120 / +0.1008 — 전부 일치.
4. 두 조건 값이 소수점까지 같으면 조작 무효 의심.

## 3. 불변식 확인
- [x] INV-A1 genn_seed 고정
- [x] INV-A2 random/np.random 시드 고정
- [x] INV-A3 d1_to_direct_weight = 20
- [x] INV-A4 d1_inhibition = -400 (`BASE` 명시, 훅이 누락 차단)
- [x] INV-A5 direct_inhibition = -100
- [x] INV-A6 evaluate 상태 정규화
- [x] INV-B1 변조폭 지표
- [x] INV-B2 무학습 대조 포함 (P·R = 가소성 전부 끔)
- [x] INV-B3 같은 시드 짝짓기
- [x] INV-B4 평가 중 가소성 동결
- [x] INV-B5 가중치 이식 평가 (`--transplant-eval`)

## 4. 사전 판정 기준
**(a) 증폭 설명 지지**: (R−P)가 **0.005 이내** AND (S−Q)가 **0.015 이상 음수**, 각각 5뇌 중 4 이상.
  → 학습이 없으면 이득 효과도 없다 = 이득은 학습을 증폭하는 것이다.
**(b) 비학습 설명 지지**: (R−P)가 **0.015 이상 음수**, 5뇌 중 4 이상.
  → 학습 없이도 이득이 행동을 바꾼다 = 변조폭 변화는 학습 지표가 아니다.
**(c) 둘 다 아님**: 중간값이면 **보류**하고 반복을 늘린다. 사후에 기준을 바꾸지 않는다.

## 5. 이 결과로 새로 알게 되는 것
- **(a)** → 병목이 **하류 증폭률**로 특정된다. H016이 주력이 되고, 다음 축은
  `d1_to_direct_weight`·`direct_to_motor_weight`(INV-A3/A5로 고정돼 있던 값들)가 된다.
- **(b)** → **우리 지표가 학습을 재지 못한다.** 가중치를 바꾸지 않아도 배선만으로 같은 크기가 나온다는 뜻.
  E069 이후 모든 행동 결과의 해석이 무너지고, 외부 검토 제안대로 **최소 회로**에서
  학습-보상 관계를 먼저 성립시키는 쪽으로 간다.
- **(c)** → 이득 효과가 학습 유무에 부분적으로만 의존한다. 두 성분을 분리하는 설계가 더 필요하다.

## 6. 이 대조가 **식별하지 못하는 것**
- **네 칸의 차이 전부**: `--kc-d1-w`(0.5/150)와 `--no-reward` vs `--real-rstdp --crossed` 둘뿐.
  KC 학습(`--kc-rstdp`)은 네 칸 모두 **끈다**. 연결 지문은 E084에서 네 칸 일치를 실측했다.
- **의도한 차이**: 위 둘.
- **지표가 관심 요인을 상쇄하지 않는가**: 관심 요인(이득)은 칸 간 축이고 지표는 이식 평가의
  변조폭 변화다. 뺄셈 양변에 들어가지 않는다.

### 이 결과로도 배제 못 하는 설명
1. **이식 목록이 학습 경로 전체를 덮는지**: 목록은 이제 `brain._rstdp_synapses`에서 **유도**하며
   이름을 못 찾은 시냅스가 있으면 예외를 던진다. 다만 `_rstdp_synapses`에 등록되지 않는
   가소성(피질/PE 갱신 등)이 있으면 여전히 빠진다. **이것은 미해결이며, 조작검증 1이
   |Δ|=0을 확인하는 범위도 이 목록까지다.**
2. **환경 1개**(`--env-seed 0`): K14의 환경별 부호 반전 전례가 있다. 일반 결론이 아니다.
3. **`--real-rstdp`와 `--crossed`를 묶어서 켠다**: 둘 중 어느 쪽이 증폭되는지는 구분 못 한다.
4. **이득 두 수준만**: 0.5와 150 사이의 형태(선형/문턱)는 알 수 없다.

---
## 실행 결과 (실행 후 작성)
- **실행 커맨드**:
- **원본 로그**:
- **결과**:
- **판정**:
- **갱신한 곳**:

---
## 조작검증 결과 (2026-09-19 17:01) — **통과**

- **실행 커맨드**: `scripts/e085_check.sh` (본실험과 같은 설정, `--episodes 5`)
- **원본 로그**: `/root/rstdp_run/e085chk/`

### 1. 가소성 정지 (`--no-reward`)
| 경로 | 학습 끔 | 학습 켬 |
|---|---|---|
| `food_to_d1_l` | 평균 1.0000 **std 0.0000** | 평균 0.2236 std 0.4999 |
| `food_to_d1_r` | 평균 1.0000 **std 0.0000** | 평균 0.2320 std 0.4585 |
| `food_to_d1_cross_lr` | 평균 0.5000 **std 0.0000** | 평균 0.1791 std 0.8676 (최대 17.31) |
| `food_to_d1_cross_rl` | 평균 0.5000 **std 0.0000** | 평균 0.1501 std 0.6099 (최대 18.31) |
| 변화율 | **0.0%** | 85.6% |

**네 경로 전부 초기값 그대로, 변화율 0.0%** → `--no-reward`가 가소성을 완전히 멈춘다. **조작 유효.**

### 1b. 이식 완전성
```
[이식] 4개 경로: food_to_d1_cross_lr, food_to_d1_cross_rl, food_to_d1_l, food_to_d1_r
```
E084에서 빠졌던 **교차 경로 2종이 포함**됐고, 학습하지 않는 `good_food_to_motor_l/r`는 제외됐다.
KC 학습이 꺼진 조건이므로 `kc_to_d1`도 제외 — 목록이 조건에 따라 자동으로 맞춰진다.

**교차 경로는 실제로 학습한다**(std 0 → 0.87, 최대 17.3).
E084는 이 학습분을 통째로 버린 채 평가했다는 뜻이다.

### 3b. 과제 경로 이식 항등성 (5시드) — **통과**
| 뇌 시드 | 0 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|---|
| 사전(직접) | +0.0973 | +0.1045 | +0.0921 | +0.1120 | +0.1008 |
| 사후(이식) | **+0.0973** | **+0.1045** | **+0.0921** | **+0.1120** | **+0.1008** |

## 본실험
- **실행 커맨드**: `bash scripts/lab/run_experiment.sh E085 "bash scripts/e085_main.sh"`
- **원본 로그**: `research/experiments/logs/E085/`
- **결과**: (실행 중, 2026-09-19 17:01 착수)

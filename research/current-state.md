# 현재 상태 (Current State)

> **이 파일은 역사가 아니다.** "지금 무엇을 믿는가"만 쓴다. 새 결과가 나오면 **덮어쓴다**.
> 무엇을 했는지는 `docs/research/DESIGN_RECOVERY.md`(append-only)에 있다.
> **새 세션은 이 파일 + `invariants.md` + `protocols.md`만 읽으면 연구를 재개할 수 있어야 한다.**

**최종 갱신**: 2026-08-31 / 근거 커밋: `697b3f5`

---

## 1. 현재 알려진 것 (강한 증거)

| # | 사실 | 근거 | 강도 |
|---|---|---|---|
| K1 | **개념 5층(조합·공간·소리·시각·일반화)은 전부 선천 배선.** 학습 기여 없음 | 짝지은 5시드: 조합 −0.9~+1.7%p, 시각 평균 −2.0, 일반화 평균 −6.1 (C47) | 매우 강함 |
| K2 | 행동은 `good_food_eye→motor`(g=25, n≈15000, **StaticPulse**) 반사가 지배 | 무학습 뇌 변조폭 0.835 vs bad 0.080, 3/3 재현 (C28b) | 매우 강함 |
| K3 | 원래 코드의 "R-STDP"는 **이름뿐**이었다: `food_to_d1`은 `_create_static_synapse`, 피질 갱신은 `w[:] += eta*trace`(전역 스칼라) | 코드 직접 확인 (C35) | 확정 |
| K4 | 체크포인트 로드가 학습 구조의 **63.6%를 평균 상수로 파괴**하고 있었다 | 55개 중 35개, 치환값이 초기값과 동일 (C24) | 확정 (수리됨) |
| K5 | **학습이 행동을 바꾼다** — 수리 후 (수리−정적) 4/5 시드가 보상 방향 | 평균 −0.0049, 귀무 대조 정확히 0 (C64/C65) | 강함 |
| K6 | 그 변화는 **누적 학습**이다 | 용량-반응 단조: 10ep −0.0007 → 30ep −0.0037 → 60ep −0.0050 (C66) | 강함 |
| K7 | 효과는 **학습률에 포화**한다 | eta 0.02→0.4(20배)에도 −0.0052/−0.0045/−0.0050 (C67) | 중간 |
| K8 | 측정 잡음: 배치 내 ≤0.0001, 배치 간 ~0.0005 | 동일 조건 3회 반복 (C68) | 강함 |

## 2. 현재 강하게 의심하는 것 (증거 있으나 미확정)

| # | 의심 | 근거 | 다음 판별 |
|---|---|---|---|
| S1 | 효과가 작은 이유는 **가중치 상한 도달** | eta 포화(K7) | E-C69 실행 중 (w_max 30 vs 100) |
| S2 | 효과가 작은 이유는 **반사(g=25·15000시냅스) 대비 학습경로 영향력이 구조적으로 열세** | 반사 0.5로 낮춰도 학습이 조향을 0.001~0.008만 바꿈 (C43) | 미설계 |
| S3 | 기저핵 변별은 **d1 탈포화 여부에 의해 결정**된다 | 탈포화 시드에서만 변별 107/38, 포화 시드는 3.5~4.6 (C58) | 부분 확인 |
| S4 | 탐색 편향을 **학습 시냅스 상류(D1)** 에 넣어야 자격흔적이 탐색 행동을 담는다 | motor 주입 시 반사 방향 강화(C50), D1 주입 3/5 역전 방향(C61, 기준 미달) | 재시험 필요 |

## 3. 현재 모르는 것

- U1. 반사를 **실제로 뒤집는** 조건이 존재하는가. (효과는 있으나 크기가 기저의 5~10%)
- U2. 선천 반사로 풀리지 않는 과제에서 **개념 4조건**(CHARTER)을 충족시킬 수 있는가.
- U3. `indirect`(NoGo) 경로가 **완전 침묵**인 이유. Go/NoGo 밀당이 작동하지 않는다 (C39).
- U4. 해마 경로(`place→food_memory→motor`, n=7562·g=5.0)가 행동에 기여하는가. 아직 **유효한 시험을 한 적 없다**(C54는 조작 무효).

## 4. 가장 중요한 열린 질문
**"학습이 행동을 바꾼다"(K5, 효과 −0.005)를 어떻게 "학습이 반사를 이긴다"(L2)로 키우는가?**
S1(가중치 상한)과 S2(구조적 영향력 열세)가 경쟁 설명이다. **먼저 이 둘을 가려야 한다.**

## 5. 활성 가설
`hypotheses/` 참조. 현재 활성: **H003**(가중치 상한), **H004**(영향력 비율), **H005**(교차경로 필요).

## 6. 다음 판별 실험
1. **E-C69**(실행 중): w_max 30 vs 100 → H003 판별
2. H003이 기각되면 → H004 판별 실험 설계(반사 시냅스 수를 줄이거나 학습경로 시냅스 수를 늘려 **영향력 비율**을 직접 조작)

## 7. 되돌아가면 안 되는 지점
- 무학습 대조 없는 성능 수치는 **보고하지 않는다** (K1이 그렇게 만들어졌다).
- 단일 런 결과로 결론 내지 않는다 (이 세션에서 3회 뒤집힘).
- `invariants.md`의 설정을 어긴 채 낸 결론은 **무효**로 취급한다 (C53이 그렇게 무효가 됐다).

# 현재 상태 (Current State)

> **이 파일은 역사가 아니다.** "지금 무엇을 믿는가"만 쓴다. 새 결과가 나오면 **덮어쓴다**.
> 무엇을 했는지는 `docs/research/DESIGN_RECOVERY.md`(append-only)에 있다.
> **새 세션은 이 파일 + `invariants.md` + `protocols.md`만 읽으면 연구를 재개할 수 있어야 한다.**

**최종 갱신**: 2026-09-16 (E079 반영) / 근거 커밋: `560e45b`

---

## 1. 현재 알려진 것 (강한 증거)

| # | 사실 | 근거 | 강도 |
|---|---|---|---|
| K1 | **개념 5층(조합·공간·소리·시각·일반화)은 전부 선천 배선.** 학습 기여 없음 | 짝지은 5시드: 조합 −0.9~+1.7%p, 시각 평균 −2.0, 일반화 평균 −6.1 (C47) | 매우 강함 |
| K2 | 행동은 `good_food_eye→motor`(g=25, n≈15000, **StaticPulse**) 반사가 지배 | 무학습 뇌 변조폭 0.835 vs bad 0.080, 3/3 재현 (C28b) | 매우 강함 |
| K3 | 원래 코드의 "R-STDP"는 **이름뿐**이었다: `food_to_d1`은 `_create_static_synapse`, 피질 갱신은 `w[:] += eta*trace`(전역 스칼라) | 코드 직접 확인 (C35) | 확정 |
| K4 | 체크포인트 로드가 학습 구조의 **63.6%를 평균 상수로 파괴**하고 있었다 | 55개 중 35개, 치환값이 초기값과 동일 (C24) | 확정 (수리됨) |
| K5 | **학습이 행동을 바꾼다** — 수리 후 (수리−정적) 4/5 시드가 보상 방향 | 평균 −0.0049, 귀무 대조 정확히 0 (C64/C65) | 강함 |
| K6 | 그 변화는 **누적 학습**이다 | 용량-반응 단조: 10ep −0.0007 → 30ep −0.0037 → 60ep −0.0050 (C66) | 강함 |
| K7 | 효과는 **학습률에 포화**한다 | eta 0.02→0.4(20배)에도 −0.0052/−0.0045/−0.0050 (C67) | 중간 |
| K8 | 측정 잡음: 배치 내 ≤0.0001, 배치 간 ~0.0005 | 동일 조건 3회 반복 (C68) | 강함 |
| **K18** | **신용 할당은 충분조건이 아니다.** KC를 시냅스별 R-STDP로 바꿔 std 0→4.33, 변화율 100%→29.8%를 달성했으나 행동 효과는 **+0.0005**(변화 없음). 병목이 **KC 하류** | E079(20런) | 강함 |
| **K17** | **KC 학습은 작동하지만 신용 할당이 없다.** 보상 100회 후 `kc_to_d1` 변화율 100%인데 **std=0.0000** — 갱신식이 `w += eta*trace*DA`로 집단 스칼라 하나를 전 시냅스에 더한다(5509행). C35 피질 학습과 같은 구조 | kc_learning_probe | 강함 |
| **K16** | **우리 배선이 생물학적 범위를 벗어난다.** FlyWire 실측 대비 연결밀도 **100~190배 과밀**, 가중치 롱테일 없음(실측 중앙값 2·p99 32·max 2405), **반사 경로 하나가 15,000 시냅스로 실제 뉴런 출력합 p99(2,955)의 5배** | FlyWire 783 | 강함 |
| **K15** | **좌우 비대칭은 환경 의존성의 원인이 아니다.** 비대칭만 제거해도 환경 간 표준편차가 0.01186→0.01233(비율 1.039). env6은 asym +10→0으로 만들어도 효과 −0.0216→−0.0215로 동일 | E076(80런) | 강함 |
| **K14** | **★대역폭 조작의 효과가 환경에 따라 부호가 반대다.** env0 **+0.0115**(~2.0 SE) vs env2 **−0.0219**(~2.6 SE). 잡음이면 양측으로 유의하게 벌어지지 않는다 | E074(60런) | 강함 |
| **K13** | **분산의 주 출처는 환경이다.** (수리−정적) 표준편차: 환경 변화 **0.0177** vs 뇌 변화 **0.0085** (2.08배). 환경 고정+뇌 반복이면 필요표본 48→11 | E073 | 강함 |
| **K12** | **★현재 측정 설계는 검출력이 부족하다.** 조건 간 차이의 **시드 간 분산이 ±0.02**인데 측정하려는 효과는 −0.005. 0.005 검출에 시드 100개 이상 필요. (C68의 '잡음 ≤0.0001'은 **같은 시드 반복**이며 시드 간 분산과 200배 다르다) | E072 | 강함 |
| **K11** | **`direct_inhibition = -400`은 D1→운동 전달을 완전히 차단한다.** D1 주입 보상 0회 vs −100에서 622회, 억제 없음에서 498~518회. `d1_inhibition`은 −400이어도 무해 | E071 조작검증 | 강함 |
| ~~K10~~ | ~~대역폭 축도 병목이 아니다~~ **보류** — 전달이 막힌 상태에서 측정됨(K11). E072 재실행 필요. 원문: 학습경로/반사 대역폭 비율을 1.0x→11.25x로 키워도 효과 −0.0052→−0.0063(차이 −0.0011, 기각기준 이내). 방향 일치 1/3 시드, 단조성 없음 | E070 | 강함 |
| **K9** | **가중치 축은 병목이 아니다.** w_max 30→100으로 가중치를 8배(4.29→32.5) 키워도 행동 불변. 게다가 w_max=30에서 가중치가 1.24~4.29로 **상한에 못 미쳐 애초에 구속하지 않았다** | E069 | 강함 |

## 2. 현재 강하게 의심하는 것 (증거 있으나 미확정)

| # | 의심 | 근거 | 다음 판별 |
|---|---|---|---|
| ~~S1~~ | ~~가중치 상한 도달~~ | **E069에서 기각** → K9 참조 | — |
| S2 | 효과가 작은 이유는 **반사(g=25·15000시냅스) 대비 학습경로 영향력이 구조적으로 열세** | 반사 0.5로 낮춰도 학습이 조향을 0.001~0.008만 바꿈 (C43) | 미설계 |
| S3 | 기저핵 변별은 **d1 탈포화 여부에 의해 결정**된다 | 탈포화 시드에서만 변별 107/38, 포화 시드는 3.5~4.6 (C58) | 부분 확인 |
| S4 | 탐색 편향을 **학습 시냅스 상류(D1)** 에 넣어야 자격흔적이 탐색 행동을 담는다 | motor 주입 시 반사 방향 강화(C50), D1 주입 3/5 역전 방향(C61, 기준 미달) | 재시험 필요 |

## 3. 현재 모르는 것

- ~~U5~~ **→ H007로 승격**(E074). 학습 효과가 환경에 의존하는가? E073에서 같은 뇌인데 환경에 따라 효과가 −0.0169~+0.0284로 **부호까지 뒤집혔다.** 순수 잡음이 아니라 환경 특성과의 상호작용일 수 있다 → H007 후보

- U1. 반사를 **실제로 뒤집는** 조건이 존재하는가. (효과는 있으나 크기가 기저의 5~10%)
- U2. 선천 반사로 풀리지 않는 과제에서 **개념 4조건**(CHARTER)을 충족시킬 수 있는가.
- U3. `indirect`(NoGo) 경로가 **완전 침묵**인 이유. Go/NoGo 밀당이 작동하지 않는다 (C39).
- U4. 해마 경로(`place→food_memory→motor`, n=7562·g=5.0)가 행동에 기여하는가. 아직 **유효한 시험을 한 적 없다**(C54는 조작 무효).

## 4. 가장 중요한 열린 질문

> **2026-09-09 변경**: "무엇이 병목인가"에서 **"어떤 환경에서 학습이 작동하는가"** 로.
> E074에서 같은 조작이 환경에 따라 정반대 효과를 냈다(K14). 환경을 지정하지 않으면 병목 질문에 답이 없다.
> 이는 CHARTER의 개념 4조건 중 **일반화**와 직결되므로 본래 목표에 더 가까운 질문이다.

**"학습이 행동을 바꾼다"(K5, 효과 −0.005)를 어떻게 "학습이 반사를 이긴다"(L2)로 키우는가?**
**가중치 축(E069)과 대역폭 축(E070)이 모두 기각**됐다. 효과(~−0.005)를 제한하는 것은 셋째 축이다.
남은 설명: **연합 자체가 잘못 형성된다**(H005). C50에서 탐색을 학습시냅스 하류에 주입하니
자격흔적에 반사정렬 패턴이 기록돼 보상이 **반사를 강화**했다.
즉 "학습이 약하다"가 아니라 **"엉뚱한 것을 학습한다"** 일 수 있다.

## 5. 활성 가설
`hypotheses/` 참조.
- **H004 (검출불가)** — E072 5시드에서 평균 +0.0035(예측과 반대), 방향 2/5. **기각이 아니라 검출력 부족**(K12). 이 설계로는 판정 불가
- **H007 (주력·활성)** — **학습 효과가 환경에 의존한다.** E074 env별 부호 반전 + E076에서 env6이 다른 3환경과 4 SE 이상 차이(원본·대칭 모두). **원인은 좌우 비대칭이 아님**(H008 기각)
- **H014 (주력·활성)** — **병목은 출력단.** D1 좌/우 2개로 수렴해 변별이 평균화된다.
  FlyWire 실측: MBON **96개**·수렴비 441·출력간 억제 **50%** vs 우리 D1 **2개**·경쟁 없음
- H013 (부분지지) — 신용할당은 생겼으나(std 4.33) 행동은 안 변함 → 필요조건이나 충분조건 아님
- **H011 (활성)** — 배선이 생물학적 범위 이탈(FlyWire 실측 대조)
- H009/H010 (활성) — 환경 의존의 원인이 난이도인가 공간배치인가. E077 진행 중
- ~~H008~~ 기각(E076, 좌우 비대칭) / ~~H012~~ 기각(도파민 미도달 — **측정으로 반증**)
- H005 (활성) — 연합이 잘못 형성된다. E071 조작검증에서 교차경로 실재 확인(n=3125/3211)
- ~~H003~~ 기각(E069, 가중치 축) / ~~H004~~ 기각(E070, 대역폭 축)
- H001·H002 지지됨

## 6. 다음 판별 실험
1. ~~E069~~ 완료 → H003 기각 / ~~E070~~ 완료 → H004 기각
2. ~~E072~~ 완료 → **검출불가**(기각 아님). 설계의 검출력 부족이 드러남
3. ~~E073~~ 완료 → **M002 지지**(환경이 분산 주 출처). 검출 설계 확보(P12)
4. ~~E074~~ 완료 → **환경 의존 갈래 발동**. H004 보류, H007 승격
5. ~~E076~~ 완료 → **H008 기각**. 비대칭은 원인이 아니다
6. **E077 (실행 중)**: env6의 무엇이 다른가. env6은 `n_good=42`(최다)·`good_ratio=0.750`(최고)·
   `nearest_good=83.6`(최근접)·`good_spread=239.8`(최소)로 **네 특성 모두 극단값**이다.
   먹이 개수만 바꾸는 식으로 **한 특성씩 인과 조작**해 원인을 좁힌다(E076과 같은 방식).
7. ~~E079~~ 완료 → **(a)신용할당 지지 / (b)행동 기각**. 병목은 KC 하류
8. **E080 (최우선)**: H014 판별. D1을 좌/우 각 4~8개 집단으로 분할 + **상호 억제** 신설
   (FlyWire MBON→MBON 억제 50% 근거). 조작검증 = d1 포화가 풀리는가.
   (구)E079 계획: H013 판별. `--kc-rstdp`로 KC→D1을 시냅스별 자격흔적 R-STDP로 바꾸고
   조작검증(도파민 OFF |Δ|=0, ON std 0→증가) 후 행동 효과 측정. **배선은 이미 완료**.
8. (보류) E075 상관 실험(37/160 중단): E077의 인과 결과가 나오면 필요 여부를 재판단.
   (구)E075 계획: H007 판별. 환경 5~8개 × 뇌 5반복으로 효과 분포를 재고,
   **환경 특성**(먹이 밀도·초기 거리·벽 근접도 등)을 측정해 **효과와의 상관**을 본다.
   상관이 있으면 H007, 없으면 H007-null(잡음).
   (구)E074 계획: P12 설계로 H004 재판정. 환경 3개 × 뇌 5반복, 대역폭 1.0x vs 11.25x.
   E070·E072가 검출불가로 끝난 것을 판정 가능한 설계로 다시 묻는다.
   (구)E073 계획:
   시드 간 분산(±0.02)을 줄이거나 효과를 키우지 않으면 어떤 가설도 판정할 수 없다.
   (해결됨: 환경 고정 + 뇌 반복)
4. (보류) E071: H005 판별 — 검출력 확보 후에. `direct_inhibition = -100`(수정된 INV-A5)에서 대역폭 스윕.
   전달이 막힌 상태의 E070 결과로는 대역폭 축을 배제할 수 없다.
   (구)E071 계획: H005 판별. 교차경로 on/off × 탐색 주입지점(motor vs D1) **2×2**, 3시드.
   조작 검증 = 교차 시냅스 n>0, D1 주입의 보상 횟수>0 확인.
   유효 설정에서 교차 on/off를 비교한 적이 **아직 없다**.

## 7. 되돌아가면 안 되는 지점
- 무학습 대조 없는 성능 수치는 **보고하지 않는다** (K1이 그렇게 만들어졌다).
- 단일 런 결과로 결론 내지 않는다 (이 세션에서 3회 뒤집힘).
- `invariants.md`의 설정을 어긴 채 낸 결론은 **무효**로 취급한다 (C53이 그렇게 무효가 됐다).
